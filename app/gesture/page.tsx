import GestureControl from "@/components/GestureControl";

const GesturePage = () => {
  return (
    <div>
      <h1>Welcome to the Gesture-Controlled Button App!</h1>
      <GestureControl />
    </div>
  );
};

export default GesturePage;
